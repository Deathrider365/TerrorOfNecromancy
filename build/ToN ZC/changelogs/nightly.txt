Changes since 2.55-alpha-120

https://zquestclassic.com/releases/3.0.0-prerelease.107+2025-05-21/

# Features

new icons
custom logo for quest package export
custom title music for quest package export
add burn-based light radii for items, to change the glow radius when lit on fire.
SCC for 'Trigger Combo Copycat', allows triggering a triggers-tab copycat index
LW_REFARROW, LW_REFFIRE, LW_REFFIRE2 (reflected arrows, fire, fire2)
Slidy Ice for the Player
'Toggle Darkness' trigger flag (toggles current room being a dark room)
'Req. Darkness' / 'Req. No Darkness' trigger flags; act as conditionals on a room being dark or not
Trigger flags relating to LItems (map,compass,etc)
Trigger options relating to tinting the palette
Trigger options relating to changing palette and wavy/quake vfx
Trigger options relating to status effects and 'Push->' timing

ZScript arrays now are separately typed from non-arrays.
  Array types can be declared by adding `[]`.

add Cure Shield Jinx to bottles
add shield effect for jinxes, update Whisp Ring
  Jinx enemies (aka Bubbles) can now impact shields, and whisp rings now correctly
  apply to temporary jinxes.
  
  See `docs/enemies.txt` for how to configure Jinx enemies.

add shield jinx removal as refill effect (#938)
add Shield Jinx trigger (#941)
expand palette from 6-bit to 8-bit color
  The RGB channels for colors have been increased to full RGB color range
  of 0-255, rather than 0-63. This increases the number of possible colors
  that palettes can use by 64x.
  
  This also impacts how colors are declared in ZScript, such as for
  `Graphics->Tint`, `Graphics->MonochromeHue` and `combodata::TrigTintR/G/B`.
  A script compat QR is added for existing quests that use 6-bit colors.
  
  A minor side effect of this change that applies for all quests is that
  palette effects, such as the fade in/out screen transitions and the
  death animation, are now smoother as there are more possible colors to
  animate through.

add configurable weapon sfx to enemies (#977)
  This can also be configured in ZScript with `npc->WeaponSFX` and `npcdata->WeaponSFX`.

fully configurable enemy weapons (#984)
  The enemy editor now allows you to configure additional things for the weapon
  that the enemy fires. These used to be hardcoded based on the weapon
  type, but now all these are configurable per-enemy:
  
  * damage
  * speed
  * block flags (ignores player defense / shields / reflectable)
  * movement flags (gravity / pitfalls / drownable)
  * tile size and hit / draw offsets
  * burning sprites and light radius

make the special sfx of enemy weapons configurable too (#991)
  Some weapon types, like boomerangs and bombs, have a secondary sfx that was hardcoded.
  You can now configure them in the enemy editor.

add enemy types to zinfo
lift glove flag to drop objects when hit
'Cycle to Screen Undercombo' combo animation flag
add Misc SFX when the Hero lands on the ground
attribute 10 now used for hop height for vires/polsvoice (#1043)
per-combo player landing sfx
  The MiscSFX landing SFX plays if none is set.

pushblocks can drown (on a QR)
combo-specific falling/drowning sprites/sfx for pushblocks
Trigger flag 'Apply Discount', allows wealth medal discount to apply to trigger counter costs
'Light Wave' effect for dark room lights
  Makes the lights increase and decrease radius in a sine wave pattern,
  with a specified rate and size. Lantern items have a checkbox
  to ignore this effect.

z3 scrolling regions (#1074)
  Regions are a collection of screens that are stitched together to become
  one playing area that the player can move around in, with the viewport
  moving to track the player. All mechanics of ZC have been updated to
  support that there can now be multiple active screens. Additionally,
  there is a new DMap option to reclaim 160 more pixels in height for the
  visible playing field (making the passive subscreen draw over it). There
  is also scripting support for configuring regions and the viewport.
  
  Here is a
  [guide](https://docs.zquestclassic.com/tutorials/z3_user_guide) covering
  what quest makers need to know about regions.

show bottom 8 pixels
  The bottom 8 pixels of the screen has always been hidden. This was
  considered to be a universally bad thing, so they are now visible. A QR
  rule is added to control this behavior, and by default all existing
  quests have been opted into it, except for those known to have issues
  related to script drawing.
  
  Active subscreens are still just 168 pixels tall. When the bottom 8
  pixels are show, active subscreens travel an extra 8 pixels when opened.
  
  ZC Player has a setting to configure this too. By default, it defers to
  the QR as defined in the quest, but users can override this.

Pushblocks can "fall" in sideview gravity
Add 'Layer' property to FFCs (ignored for 'Overhead' ffcs)
add "screen load" combo trigger (#1080)
Lifted combos specify glow shape / radius
zc: new logo and song to file select screen
zc: add option to disable title music
zc: optimize zasm scripts
  First thing when loading a quest the ZASM scripts will undergo
  optimizations. Most optimization passes apply improvements similar
  to that which have been implemented in the zscript compiler, but are
  not present in older quest scripts. Others are novel and improve even
  modern zscript compiler output.
  
  Savings for a few quests:
  
  - Stellar Seas - 163762 ops (5%)
  - Yuurad       - 604668 ops (4%)
  - Umbral Cloud - 105994 ops (8%)
  
  The optimizations done currently are:
  
  - Removing unreachable blocks of code
  - Replace many PUSH/POP with equivalent single PUSHARGSR/POPARGS
  - Reduce ops used for conditionals (COMPARER)
  
  This is on by default, but can be disabled with
  `[ZSCRIPT] optimize_zasm = 0` in `zc.cfg`.

zc: add zasm optimize_setv_pushr
zc: optimize comparison commands in ZASM
zc: optimize scripts just before use, instead of all upfront
zc: in optimizer, consider the null D5 register when combining commands like POP
zc: optimize old scripts LOADI to LOADD
  This removes ~8% of commands from Yuurand, ~12% from Nargad, and ~14%
  from Stellar Seas.

zc: optimize useless stack push/pops in old scripts
  This saves 15% of instructions for Yuurand.

zc: add simple titlescreen, and remove music from save select screen
  This also reintroduces the `skip_title` option in the launcher.

zc: remove dead ZASM
  ZASM ops that write to a register but never use the result are now
  removed.

zc: continue ZASM comparison reduction even on LOAD D2
  At some point this was necessary, but not anymore.

zc: improve inference in optimizer simulator, and handle POP better
zc: improve ZASM comparison when subsequent block reuses D2
zc: optimize SETR PUSHR to just PUSHR
zc: optimize zasm pass converting to new function calls
  Replaces older GOTO/GOTOR and GOTO/RETURN with more succinct
  CALLFUNC/RETURNFUNC.
  
  This saved ~2.5% of instructions in Yuurand, ~4% in Nargads Trail.

zc: optimize zasm by inlining functions
zc: optimize old scripts STOREI to STORED
zc: option to show game time in the corner, for all those speedrunners out there
zc: optimize LOADD/STORED to new LOAD/STORE to remove divisions
zc: optimize zasm by removing intermediate registers
  - For example, `SETR D3 LINKX ; TRACER D3` becomes `TRACER LINKX`
  - Also attempts to avoid pushing value to stack just to pop later,
    but only if the value of the register is unmodified in the original
    code between pushing and popping from the stack
  - Only occurs if the register is read once. If read twice, the overhead
    of calling `get_register` is too great so it would be worse to call
    multiple times. Data registers are much faster to access.

zc: 'Toggle Lights' cheat works on new dark rooms
zq: new song to ambient music menu
zq: Merge 'Favorite Command' and 'Hotkey' systems
  Any 'Hotkey' can now be set to a favorite command, and all old favorite commands now have 'Hotkey's.
  Favorite command buttons will clear upon this update, as the configs have changed.

zq: save backup when qst was last saved in a different version of zc
zq: 'Adv. Paste' string editor rclick option
zq: Advanced Paste for Item Editor
zq: 'Warp Back' rclick option to reverse after following a warp
zq: Item Wizard (Shields)
zq: assign 'notes' per-screen (just for editor use)

  Relevant changes:
    feat(zq): 'Set Default' button to set default editbox colors
    feat(zq): 'Goto' button in 'Browse Notes' to go to a notes' screen
zq: export/import strings to .tsv text file
  Allows for exporting strings to a spreadsheet, text editor, or other
  program for modification, then importing them back to ZC.
  
  The first row of the `.tsv` (tab separated values) file are the column
  names, followed by an example row to designate where the first three
  lines begin and end for each string (but you can add more lines than
  that).
  
  For best results in a spreadsheet, "Freeze" the first two rows and use a
  monospace font for the "message" column.
  
  Example:
  https://docs.google.com/spreadsheets/d/197-NSvNQJ174Ri9GVlxYDX4oFp8w1SZbmOXuDohKZZk/edit?usp=sharing

zq: add help text to the FFC editor (#973)
zq: zoom out to edit many screens at the same time
  It is now possible to zoom out when editing map screens, up to the size
  of the entire map (although 4x4 screens is roughly the last usable zoom
  level).
  
  `Ctrl/Cmd + -/+` is the shortcut to zoom out and in. Note that this
  replaces an existing shortcut for modifying the screen palette by 16 -
  that is now `Shift + Ctrl/Cmd + -/+`.
  
  Arrow keys change the current screen as usual (and so updates all the
  screen-specific UI, like enemies/warps/the Screen menu). If you move
  beyond the current viewport, the viewport moves along with you.
  
  Clicking on any visible screen will also set the current screen.
  
  Should be able to perform any action as expected on screens when zoomed
  out: copying, pasting, ffcs, moving items, warp squares, all drawing
  modes, flood fill, editing layers, etc.
  
  The undo/redo system works as well.
  
  There are a few new options related to this feature, under the View
  menu.

zq: Check/Uncheck/Toggle All buttons for checklist dialogs (ex. adv paste)
zq: add 'Alphabetized' checkbox to basic listers (item,enemy,sfx,midi,subscrwidget)
zq: add '(000)' after '(None)' in some lists, allowing typing '0' to skip to it.
zq: (enemy editor) added descriptive labels and dropdowns to walker type attributes (#1041)
zq: 'Drag Squares From Center' option, for FFCs/warp squares/etc
zq: Tools->Fix Tools->Green Arrival Square Fix
  Allows you to clear out all the green arrival squares in your quest (and optionally turn them into blue squares)

zscript: more value range types (.., =.., ..=, =..=)
zscript: allow mathematical range expressions [A,B) etc
zscript: range loops (`loop(int q : 0=..10)`)

  Relevant changes:
    feat(zscript): range loops can contain variables in ranges/increment, and negative increments
    feat(zscript): range loops have '@NoOverflow()' annoation, docs
zscript: allow multiple comma-delimited increment statements in for loops
zscript: allow enums of non-int pre-existing types (ex. `long`)
zscript: add networking via websockets
  WebSockets allow for scripts to communicate with an external WebSocket
  server. This introduces an official way to use the network in ZScript.
  
  The following example script communicates to a freely hosted WebSocket
  server, which simply responds back with the messages it receives.
  
  To make your own WebSocket server, Python is a good option. Some example
  libraries:
  
  - https://github.com/Pithikos/python-websocket-server
  - https://websockets.readthedocs.io/en/stable/
  
  ```c
  #include "std.zh"
  
  generic script WebSocketScript
  {
      void run()
      {
          printf("Connecting to websocket.\n");
          websocket ws = Game->LoadWebSocket("ws://ws.ifelse.io");
          ws->Own();
          bool hasSentMessage = false;
  
          while (true)
          {
              if (ws->State == WEBSOCKET_STATE_CONNECTING)
              {
                  Waitframe();
                  continue;
              }
              else if (ws->State == WEBSOCKET_STATE_CLOSED)
              {
                  char32 error[0];
                  ws->GetError(error);
                  printf("Failed to connect: %s\n", error);
                  break;
              }
  
              if (ws->State != WEBSOCKET_STATE_OPEN)
              {
                  printf("Failed to connect: %l\n", ws->State);
                  break;
              }
  
              if (!hasSentMessage)
              {
                  printf("Connected!\n");
                  ws->Send("hello world!");
                  ws->Send("domo arigato!", WEBSOCKET_MESSAGE_TYPE_BINARY);
                  hasSentMessage = true;
              }
  
              while (ws->HasMessage)
              {
                  int message_ptr = ws->Receive();
                  int type = ws->MessageType;
                  if (type == WEBSOCKET_MESSAGE_TYPE_TEXT)
                      printf("got text message: %s\n", message_ptr);
                  else if (type == WEBSOCKET_MESSAGE_TYPE_BINARY)
                  {
                      int len = SizeOfArray(message_ptr);
                      printf("got binary message of length: %d\n", len);
                      for (int i = 0; i < len; ++i)
                          printf("%d: %l\n", i, message_ptr[i]);
                  }
                  else
                      printf("got message, but with unknown type: %l\n", type);
              }
  
              Waitframe();
          }
      }
  }
  ```

zscript: 'atol' (string to long)
zscript: add template types to some internal functions
  The following internal functions have switched from using `untyped` to
  template types:
  
  ```c++
  void ArrayPushBack(T[] arr, T val)
  void ArrayPushFront(T[] arr, T val)
  void ArrayPushAt(T[] arr, T val, int index)
  T ArrayPopBack(T[] arr)
  T ArrayPopFront(T[] arr)
  T ArrayPopAt(T[] arr)
  void ArrayCopy(T[] dest, T[] src)
  int SizeOfArray(T[] arr)
  void ResizeArray(T[] arr, int sz)
  void OwnArray(T[] arr)
  void DestroyArray(T[] arr)
  
  void printfa(str* format, T[] args)
  void sprintfa(str* buf, str* format, T[] args)
  
  T Min(T arg1, T arg2, ...T rest)
  T Max(T arg1, T arg2, ...T rest)
  T Choose(T arg1, ...T rest)
  ```
  
  This means that the type `T` must match other occurrences of `T`
  throughout the entire signature, given the types of the parameters used
  by the callsite. For example, this code no longer compiles:
  
  ```c++
  printfa("hello %d", 1) // Second param is not an array
  Object objects[] = {new Object()};
  ArrayPushBack(objects, 1) // Second param is not an Object
  int v = ArrayPopBack(objects) // Cannot convert Object to int
  ```
  
  Note: for now, `SizeOfArray` and `ResizeArray` still allow `char32` for
  their array parameters, since that is common for string operations and
  we don't have a formal string type yet.

zscript: add garbage collector for automatic memory management
  Objects created by scripts are now automatically deleted when they are
  no longer reachable from any script.
  
  This removes needing to manually delete objects you create (either with
  the `delete` operator or `Free()`), and mostly removes any reason to
  ever call `Own()`.
  
  See the new "Object Memory Management" section in ZScript_Additions.txt
  for further details.

zscript: option 'OLD_ARRAY_TYPECASTING' to use old array type handling
zscript: add LEGACY_ARRAYS option as more lenient approach to array syntax
  This replaces the `OLD_ARRAY_TYPECASTING` option with `LEGACY_ARRAYS`.
  It can be set to `on`, `off`, or `warn`, and defaults to `warn`. This
  option supports usage of deprecated syntax for arrays. This will
  continue to work:
  
  ```
  int arr = {1, 2};
  Trace(arr[0]);
  ```
  
  * by default, this will warn that `int[] arr` is the preferred syntax
  * legacy arrays syntax is currently not allowed for objects that must be
    free'd, such as custom classes or `bitmap` (but, for example,
    `lweapon` is fine)
  
  Additionally but independent of this option, parameter types are cast
  to/from arrays when resolving a function call. There was an existing
  feature that selects the function that required the fewest type casts,
  and this taps into that. For example, this code works:
  
  ```
  void fnArray(int[] arr) {}
  void fnLegacyArray(int arr) {}
  
  ...
  
  int legacy_arr = {1};
  fnArray(arr); // arr is cast to int[]
  int arr = {1};
  fnLegacyArray(arr); // arr is cast to int
  ```

zscript: 'TileBlit' and 'ComboBlit', for drawing parts of tiles/combos
zscript: webapp for exploring and sharing ZScript
  https://web.zquestclassic.com/zscript

zscript: add bottle cure shield jinx flag (`BTF_CURESHIELDJINX`)
zscript: add `Game->Exit()`, which closes the program
zscript: add base `sprite` class
  The following classes now all share a common base class:
  
  * eweapon
  * lweapon
  * ffc
  * itemsprite
  * npc
  * player
  
  All of these classes can now be assigned to variables of the type
  `sprite`. The `sprite` class contains 40 properties that were duplicated
  across these classes. Some properties do not work for all classes (like
  `ffc` and `player`), but more support may come later.

zscript: show source code context with error messages
zscript: add `RepairShield` function to npc
zscript: support setting `Hero->ScriptTile` to draw larger than 1x1
  Internally, the Hero sprite resets its `Extend` property every frame,
  which prevented using `ScriptTile` in combination with `TileWidth` and
  `TileHeight`. Now, `Extend` is only reset if `ScriptTile` is not in use.

zscript: `@Increment` annotation for enums
  Allows manually setting a value for the enum to increment by for each value

zscript: add `@Bitflags` enum annotation (#1064)
  This annotation turns an enum into a bitflags enum:
  
  1. Auto-incrementing values starting at 1 and doubling the previous for
     each new enum member
  2. For type safety, bitwise operations on enum bitflags are only allowed
     with the same type
  
  For example:
  
  ```
  @Bitflags("int") // "long" is also supported
  enum SomeBitflags
  {
  	Fast,
  	Big,
  	Loud,
  };
  
  @Bitflags("int")
  enum OtherBitflags
  {
  	Combo,
  	Enemy,
  	Weapon,
  };
  
  auto x = Fast | Big; // inferred type is SomeBitflags
  if (x & Enemy) // Errors, scripter accidentally used the wrong enum.
  	// ...
  ```

zscript: add `sprite::SpawnScreen`
  This is the screen that the sprite spawned on. In a non-scrolling
  region, this is the same as `Game->CurScreen`.

zscript: support expression in default parameters
  The default for optional parameters no longer need to be constant. For
  example, this is now valid:
  
  ```
  void debug(int num, int x = Player->X, int y = Player->Y - 10) {
  	// draw num at x, y ...
  }
  ```

zscript: add `ComboAdjust` for getting combo position offset by some pixels
zscript: support passing string / boolean to `Trace`
zscript: add `ReadBitflags` and `AdjustBitflags` helper functions
zscript: add `Flag[]` to `Screen` and `mapdata`
  The new `Flag[]` variable is a boolean array of all the flags associated
  with a screen. This replaces `Flags[]` and `EFlags[]`, which are more
  complicated since they grouped bitflags by category and required bitwise
  operations to access individual flags.
  
  ```
  // Old
  bool v = Screen->Flags[SF_ROOMTYPE] & (1 << SFR_SIDEVIEW);
  // New
  bool v = Screen->Flag[SFL_SIDEVIEW];
  ```
  
  Because of their complexity, `Flags[]` and `EFlags[]` never supported
  writing to it. `Flag[]` makes that possible now.
  
  Also removes `GetMapscreenFlag` and similar helper functions, as usage
  was low/non-existent and `Flag` totally replaces it.

vscode: document symbol outline, hover tooltips and "Go to Definition"
  This patch greatly enhances the capabilities of the language extension.
  New features are:
  
  * A symbol outline of the current script, showing all variables,
    functions, classes, namespaces, etc.
  * Tooltips on hover, shown if a variable/class/whatever was declared
    with a comment. This supports markdown
  * "Go to Definition" to jump to where something is defined. You can
    right-click to see this option, or CTRL/CMD+click on something

vscode: support @link tags in hover tooltips
  Comments may contain inline links to other symbols with the `@link` tag:
  
  ```c++
  // Better than {@link counter2} ... {@link counter2|that counter is lame}
  int counter1;
  // This also works as shorthand for links: [counter1]
  int counter2;
  ```
  
  When hovering the mouse over any usage of `counter1`, the tooltip will
  render these as clickable links that navigate you to the specified
  symbol.

vscode: support Go To Definition for type of variable declaration
vscode: support Go To Definition for includes/imports
vscode: support Go-To-Definition for enums
vscode: support Go To Definition for function return type
  Also support tooltip showing up when hovering over declarations.

vscode: support 'Find all references'
  For now, this is only able to list all references within the current
  file.

launcher: add options for window size, and cap max default window size
  `[ZLAUNCH] window_width, window_height` in zcl.cfg
  
  Cap max default size to 2x

web: first pass on wasm JIT backend for web
  Adds a new backend to convert ZASM to WebAssembly. This also includes
  some utility methods for analyzing the structure (finding the functions)
  and control flow of a ZASM script, which was necessary to split ZASM
  scripts into multiple WebAssembly functions and implement the control
  flow of a ZASM script into WebAssembly structure control ops.
  
  Although `prime.zs` is ~10x faster, scripts from real quests aren't any
  faster than the normal ZASM interpreter, since a very naive and poorly
  performing method was used to transform the ZASM control flow graph.
  
  The utility methods for analyzing ZASM should be useful for improving
  the x64 backend too, for example, to similarly compile only one function
  at a time.

web: support more ZASM ops for wasm jit
web: support new SETCMP, GOTOCMP, CALLFUNC, RETURNFUNC ops in wasm

# Bug Fixes

fairy npc not despawning when picked up
changer ffcs counting for trigger groups and similar effects (they shouldn't)
zfix modulo operator being entirely wrong
changer ffcs counting for trigger groups and similar effects (they shouldn't)
zscript docs were not being generated for release package
'Screen->Lit' not compiling; now properly returns (and sets) screen's lit state
remove sfx limit (except for fuzz testing)
initial path not being set for file dialog
pushblocks ignoring the top half of combo solidity

Fire level triggers not counting as 'weapon' triggers
  They didn't count for effects such as 'Kill Triggering Weapon'- they do now.

zc: cheats menu being greyed out when it shouldn't be
zc: use correct sav path for -standalone mode
zc: use more performant throttleFPS instead of rest in system menu
zc: only assign save path just before actually writing to disk
zc: prevent calling dmap script twice in scrolling dmap warp
zc: not showing error dialogs in title screen, and unbreak -only switch
zc: prevent failing to save after continue
zc: disable scripts that start with an unknown command
zc: check for null when printing ZASM command string arg
zc: improve function breakdown in structured zasm
zc: handle first command being function call for structured zasm
zc: handle recursive function calls in ZASM cfg creation
zc: detect dtors as separate functions in structured zasm
zc: remember last save slot position when returning to save select screen
zc: re-optimize when jit reuses scripts from last load
zc: invalid optimization on broken SDDDD register
zc: disable propagate_values and dead_code passes for now
  These are likely to have a few edge cases remaining.

zc: do not stretch GUI
zc: skip title screen if specific save slot is given
zc: actually stop mp3/ogg, and explicitly stop title music
  The titlescreen music would restart in the file select screen if the
  Sound dialog was opened then closed. This was because the volume was set
  to 0 to fade out, but it was never actually stopped.
  
  But also, due to a bug ogg and mp3 formats were really just "paused"
  when told to stop, so setting the volume afterwards would result in
  resuming music that was meant to be over.

zc: inline function optimization handles PEEK correctly
zc: various fixes to optimizer that broke crucible quest
  - ignore QUIT in inline opt
  - handle POPARGS in inline opt
  - add missing register dependencies for multiple commands

zc: inline function optimization handles PUSHARGSR correctly
zc: various slope improvements, like fixing ffc slopes
zc: save select screen not showing new hp after save->reset
zc: load default quest for sfx when loading replay
  Quests before custom sfx rely on the default quest providing sfx,
  but that wasn't being loaded when playing back a replay. So it would
  either use whatever the last quest loaded, or have nothing, which was
  the case for the replay test script.

zc: websockets reading wrong pointer
zc: bug in dead code optimizer when register is both read and written to
zc: passageways can have their own midi music (#934)
zc: shadows for rocks and boulders not drawing (#951)
zc: ensure enemy projectiles spawn at center of hitbox (#994)
zc: ignore conveyors when rafting
zc: pushblocks with non-push flags placed on them were leaving trails of that flag
zc: layers not showing revealed secrets in map view
zc: 'Platform' ffcs teleporting the player as the screen loads
zc: Game time shows in-game, even when you've 'cheated'
zc: inline function optimization handles LOAD correctly
zc: apply switch state when checking bordering screens
  For example, the smart scrolling feature which prevents scrolling when
  detecting solid combos on the next screen was not considering the state
  of level or global switches.

zc: use proper start screen after reset for test mode + recording a replay
zc: prevent crash on `Game->Reload()` or instant reload
  Regresseed in 2.55.7

zc: hover boots duration being halved over pits
zc: don't skip saving game in test mode
  Methods of saving the game other than directly via them menu (such as
  autosave on screen entry, or via scripting) were not saving any data
  when in test mode. Now, continuing or otherwise reloading the current
  test mode session will persist those saves.

zc: don't stop recording in test mode on quit
  Only the "Continue" option correctly continued recording when ending the
  game in test mode, while the other options would end the recording and
  start a new one. Now, it continues as one recording.
  
  "Game > Reset" still restarts the recording in test mode.

zc: lifted weapon not disappearing on death
zc: subscreen items no longer hidden after collecting triforce
  If the "No Cutscene" flag is checked for the triforce item, collecting
  the triforce was incorrectly hiding subscreen items indefinitely.

zq: missing rclick menu separators
zq: test init data not being applied correctly
zq: handle quest path/title with spaces in package export
zq: handle error instead of crashing if package export fails
zq: 'Notes' and 'Browse Notes' not having menu buttons
zq: upgrade tile 'move' code, more things now update on moving tiles (#916)
zq: crash on moving combos in quests with many maps/screens
zq: Improve efficiency of combo moving + fix crash + fix 'undo' combo move
zq: Improve efficiency of tile moving + fix 'undo' tile move
zq: memory issue when moving/inserting tiles/combos
zq: arrow buttons above combo columns sometimes not working properly
zq: 3-stair warp dialog not properly listing catchall purpose
zq: buffer overflow in info editor dialog
zq: reduce latency on combo page paste when ComboProtection is off
zq: prevent crash when assigning global scripts
zq: delete the initial screen from default.qst
zq: prevent another crash when assigning global init script
zq: draw eyeball combos correctly in compact mode and when zoomed out
zq: add latest fields to .znpc for enemy save/load (#1003)
zq: fix tile protection for darknut, gleeok, ganon, and patra enemies (#1016)
zq: combo editor triggers -> Level Palette now has hex/decimal swapping button
zq: aliases with layers set not placing correctly when zoomed out
zq: prevent crash when generic script data length is 1
zq: Clicking snapshot option in menu capturing the menu itself
zq: Update cambria tileset
  Fixed the falling / drowning animation for enemies
  Made bomb flower combos interact w/ weapon triggers
  Added shatter animations for pots when you slash them
  Fixed solidity for combos 3916 and 3917
  Made fairies catchable with the Bug Net
  Gave the Bug Net and use sound


  Relevant changes:
    fix(zq): Update cambria tileset
zq: `Step->Effects` combo wizard 'Damage' off by *10000
zq: View Map cutting off screens in fullscreen mode
zq: changing resolution in View Map via space bar
  Changing resolution via Space resulted in a bug where the viewer would
  incorrectly use the wrong limit for the panning.

zq: update Cambria tileset
  - Fix CSet errors in Door Combo Sets
  - Fix accidentally uncombo'd tiles
  - Fix locked door combos using the lock block combo type, which can
    conflict with engine locked doors

zq: include licenses in package export
std_zh: clean up some missing function returns
std_zh: use template for `Cond` to support arrays
  Also deprecate and suggest using ternary expressions.

std_zh: `AdjacentCombo` now returns -1 for invalid combo position
zscript: errors related to breaks in infinite loops
zscript: issue with global variables sometimes not initializing properly
zscript: label error bug with 'while(true)' and 'break;'
zscript: case-range backwards check
zscript: missed handling arrays for unused var trimming
zscript: clear script data when reassigning script
  Previously, only FFCs properly reset their state when changing their
  script. All others would retain their state, so would instantly do
  all the wrong things.

zscript: exit with correct status for jit on QUIT
zscript: consider PEEK when finding function calls for structured zasm
  The structured zasm builder assumed all function calls are followed by
  `POP D4` in older scripts. However, if `POP D4` is followed by `PUSH D4`
  then the compiler replaces it with `PEEK D4`. This oversight missed
  these function calls, which broke the JIT compiler and ZASM
  optimizations for newer 2.55 compiled scripts.

zscript: use correct config section for optimize_zasm
  The base config was using the wrong value, so it was still always on.

zscript: handle WAITX not actually yielding in JIT compilers
  `WAITFRAMESR D2` does nothing when D2 is 0, but the JIT compilers assume
  all wait commands should yield. Now they check if waiting actually
  occurs.

zscript: x64 jit compiles STACKWRITEAT correctly
zscript: invalid compiler output from removing unused variables
  The compiler has a pass to remove unused variables from taking up a
  position on the stack. However, a logic error made it so the wrong
  scopes would have their stack variables repositioned, which resulted
  in multiple variables sharing the same stack position, and breaking the
  script.

zscript: parser bad optimization edge-case
zscript: parser failing to properly initialize some variables
zscript: crash in UserDataContainer, broken `websocket->Free()`
zscript: websocket double-deleting their arrays, doing extra alloc/dealloc work
zscript: bad optimization causing global vars to sometimes read incorrectly
zscript: websocket pool being destroyed on 'cont_game()'
zscript: socket arrays sometimes going invalid?
  Patched the issue here, not the cause. Not sure what caused it to sometimes go invalid, but a validity check seems sane.

zscript: derive language runtime version from compiled scripts
  The script engine checks the version a script was compiled in to vary
  some behavior. Previously this version was set to the section version
  in the qst file format, but that is updated even if scripts were not
  re-compiled. Now we look directly at the script metadata to set this
  version. This avoids issues with saving a quest in a new version without
  a recompile.

zscript: don't only show singular deprecation warnings
zscript: handle non-global objects correctly when reloading game
zscript: allow simple types to match a template array type, for now
zscript: 'Hero->Warp()' using current screen's warp return square value
zscript: stack offset issue when 'return'ing inside a 'loop()'
zscript: not correctly removing stale pointers on reload
zscript: '@AlwaysRunEndpoint("off")' erroring despite being valid
zscript: exit process when preprocessor recursion limit is reached
zscript: object fields not being available in dtor
  This regressed because of the recent GC changes.

zscript: compiler crashing on for-each loops
  This regressed because of the recent error locations refactor.

zscript: some bindings ZASM using wrong literal values
zscript: 'loop()'s not having location metadata associated with their identifier
zscript: 'hero' and 'link' script types not compiling
zscript: code gen for range loop using wrong op
zscript: 'OwnArray()' not working properly at all
zscript: code blocks sometimes not removing object references
zscript: crash from metadata of var inside if statement
zscript: continue generating editor metadata after (most) compile errors
zscript: generate metadata for code even if optimized away
zscript: scope error when using `for (...){} else {}`
zscript: prevent bogus "invalid object pointer" caused by uninitialized memory
zscript: remove a bunch of unnecessary zprints
zscript: make `npcdata->Flags` a bool array, `remove ->Flags2`
  `npcdata->Flags` and `npcdata->Flags2` are undocumented and broken tools
  for accessing the internal flag of npcdata. They tried to split 64
  possible flags over two integer values, but since they aren't "long"
  ints it didn't work. It is now a bool array.

zscript: fix broken array literals when declared as array pointers
  In ZScript, there are array declarations and array pointers. Array
  pointers are used as array parameters, and declarations are (typically)
  used to declare arrays (either with an initializer list, or as an empty
  array).
  
  Array declarations register their array to be deallocated at the end of
  the scope. Array pointers can take an array, and won't deallocate it
  when it goes out of scope. That's why pointers are used in parameters.
  
  This syntax is easy to mix up, so one might declare an array using the
  pointer syntax. The compiler would not complain, but it would actually
  produce invalid code.
  
  ```
  item items1[] = {item}; // array declaration syntax, worked
  item[] items1 = {item}; // array pointer syntax, was broken
  ```
  
  This is a recent-ish regression in the 3.0 branch, and did not happpen
  in 2.55. This has now been resolved.

zscript: pop parameters for internal functions in fewer commands
zscript: typo in `Region->GetScreenForComboPos`
zscript: for-each loop compile issue when iterating over 'const' arrays
zscript: `Game->GameOverScreen[]`/`Gam->GameOverStrings[]` not compiling in 3.0
zscript: deprecation warnings for functions not giving 'INFO:' text
zscript: prevent crash when processing for-loop metadata
zscript: use lexical scoping for namespace compiler options
  Previously, namespaces would ignore options defined in the file scope.
  Also, options defined in one namespace block would be applied to every
  namespace block of the same name.
  
  See https://discord.com/channels/876899628556091432/1243749050616905768.

zscript: internal bitmaps from `Game->LoadBitmapID()` now work with Blit
zscript: `Screen->DrawLight` functions now respect `Screen->DrawOrigin`
  These functions did not add the playing field offset, like other draw
  functions do. Now they respect `Screen->DrawOrigin`, and use
  `SPLAYER_DARKROOM_UNDER` for the draw timing.
  
  Old behavior is behind the compat QR `Scripts Screen->DrawLight
  Functions Have No Offset`.

zscript: `lweapon->isValid()` now accurate for lifted weapons
zscript: remove `Integer`, `Short`, `Byte`, etc. functions
zscript: use correct id for `eweapon::TotalDYOffset`
zscript: always deallocate local arrays when script ends
  When the bugfix QR `Always Deallocate Array` is off, local arrays are
  sometimes not deallocated when FFC/item/global scripts end. This can
  quickly hit the limit for the number of arrays allowed, and results in
  bad things.
  
  If a local array pointer is passed to another script without changing
  ownership, this QR being off prevents it from being deleted. Otherwise,
  as long as the calling script stays alive that array remains valid.
  
  There is no known quest that relies on this behavior - so this QR now
  has no impact and local arrays owned by a script are always deallocated
  when it ends.
  
  Currently, the appropriate way to keep a local array alive beyond the
  life of the script that created it is to call `OwnArray` in some other
  script.
  
  https://discord.com/channels/876899628556091432/1373883674361335829

vscode: character literals not highlighting properly
vscode: 'AlwaysRunEndpoint' annotation missing syntax highlighting
vscode: 'loop' keyword missing highlighting, add range operators to operator list
vscode: invalid uri on windows for links in hover tooltip
vscode: increase max buffer size for zscript process, which was breaking editor for large scripts
web: improve sfx on title screen by yielding during quest load
web: handle reaching end of script in wasm jit
web: handle CMP_BOOL in jit wasm
web: skip titlescreen for quick open links
web: disable broken parallel processing for zasm opt
web: avoid performance hit by disabling info bitmap
web: packaged quests not loading correctly
web: disable text selection in Safari

# Documentation


create https://docs.zquestclassic.com
  This is a new website for documentation. It's currently mostly just
  reference docs for ZScript, but soon it will also have tutorials and
  documentation for the editor.

add SCC tutorial
remove ghost, tango doc sites from release package
remove zasm_registers.txt from release package
add new zscript language documentation (#1061)
update docs related to generic script events
zscript: update webdocs
zscript: use .. version*:: admonitions for '@version' docs

# Build

include logo and title assets in web data file
fix websocket cmake project from breaking CMAKE_CONFIGURATION_TYPES

add default file for compiler "signature"
  This is to make the build _just work_ in a clean checkout until I get
  around to removing this pointless thing altogether.

fix typo in building.md for cmake version
use better library for json

  Relevant changes:
    ci: update to macos-13 in other places too
update fmtlib to 10.2.1
update building.md for Linux
pin poolSTL dependency to specific commit
update websocketpp to newer develop branch
fix gcc compilation issues
only depend on .git/refs/tags if present
[linux] do not disable allegro5 png, which broke the title screen logo
update libpng to 1.6.47, zlib to 1.3.1
update fmtlib to 11.2.0
web: upgrade to emscripten 3.1.60
web: upgrade to emscripten 3.1.64
web: upgrade emscripten to 4.0.8

# Refactors


enable allegro DEBUGMODE in debug builds
  This enables allegro asserts, among other things. zc_alleg.h attempted
  to enable for _every_ build type, but it actually had no effect since
  allegro is built as as separate target.

Menus (new sleeker look, much cleaner backend)
share common allegro app setup
load text files more efficiently
remove scary and now pointless warnings when toggling fullscreen
use enums instead of defines for many flag values
combine misc attributes for guys into array (#955)
use vector for storing FFCs
  Previously, every screen allocated memory to hold the maximum number of
  FFCs (2.55 alpha 112 bumped this from 32 to 128), which took ~43 KB per
  screen, even if no FFCs were used. Now screens only allocate as many
  FFCs as they need.
  
  For a quest that uses no FFCs at all, this saves ~5 MB of memory per
  map. For Yuurand, total memory usage on startup went from 1.3 GB to 800
  MB.

read weapon names from zinfo, not the deprecated module system (#1022)
consistently use "Hero" instead of "Player" to refer to the playable character
move Hero extern to header
move particles extern to header
remove module system (zinfo has replaced it)
rewrite string message parsing and renderer
  * Completely rewrite the string message parser / renderer - this will
    make it possible to add more advanced SCC features in the future.
  * Executing a SCC no longer takes a frame per command.
  * Use the same code for parsing a message string in the player and in
    the editor - this fixes a handful of differences w/ word wrapping that
    the player handled poorly.
  
  https://discord.com/channels/876899628556091432/1277878877057978460

zc: add null jit backend
zc: move debug zasm writing to new -extract-zasm command
  This allows for all scripts in a qst to be written to disk, instead of
  only the ones that happen to execute.

zc: use common structured zasm utilities in jit_x64.cpp
zc: simplify how structured zasm finds functions
zc: split some zasm opts into separate script-level pass
zc: assume modern function calls in zasm optimization passes
zc: move get_register_dependencies to zasm_table.cpp
zc: remove music buffer setting
  This only applied to it/xm/s3m/mod music. There's no need to expose this
  to users. Instead, always use 128KB as a buffer size.

zc: add get_command_implicit_dependencies in zasm_table.cpp
zc: add get_register_ref_dependency in zasm_table.cpp
  Also run `propagate_values` after the comparison opt passes, as it was
  accidentally skipping some optimizations due to the comparisons passes
  assuming D2 is being compared.

zc: simplify zasm simulation and always simulate stack
zc: optimize zasm scripts in parallel
zc: improve handling of bugged SDDDD by fixing it, instead of deopting
zc: reduce duplicated code for trigger conditionals

  Relevant changes:
    refactor(zc): reduce duplicated code for trigger results
zc: clean up jinx handling (#939)
zc: deduplicate enemy code reading of SIZEflags
zc: remove old movement functions
zc: use transulency table cache for all fade/lighting
zq: Clean up Tile/Side/Ring warp dialogs
zq: re-organize the combo editor 'Triggers' tab
zq: redesign enemy editor using new GUI (#971)
zq: upgrade enemy editor's enemy selector to new gui (#986)
zq: remove the `Misc[]` dialog as it was unused
zq: upgrade screen enemy selector to new gui (#989)
zq: upgrade midi editor to new gui (#995)
zq: upgrade sfx editor to new gui (#996)
zq: upgrade map styles editor to new gui (#997)
zq: in enemy editor, read defense names from zinfo
zq: remove classic_zquest.dat, load assets from new assets/editor folder
zq: clean up `Quest->Audio->MIDIs` lister
zq: rename Fire Levels to Burning in combo triggers tab
zq: upgrade sprite data list dialog
zq: drop "Load Tileset" and consolidate loading tilesets via File>New
std_zh: remove instances of deprecated functions
std_zh: make every std file compile individually
std_zh: move some constants from std to bindings, and add more doc comments
zscript: internal function aliases, internal constexpr functions
zscript: use alias name in comments, update expected zscript
zscript: reserve 'constexpr' token
zscript: clean up ScriptEngineData for all script types
zscript: clean up generic script indexing
zscript: optimize bool ops a lot
zscript: clean up getStackOffset(Datum) code
zscript: label opcode register args as R/W/RW/unused
zscript: label some opcode properties with flags
zscript: use common UserDataContainer for most user data
zscript: standardize script object management
  This keeps script-created objects in the same map, gives all objects
  a unique id, and implements allocation and retrieval for any object type
  using the same methods. This reduces some duplicated code, and should
  help with implementation of a potential garbage collector feature.
  
  Bitmap objects are a bit special, and still need to be refactored.

zscript: document, clean up, and sanity check websocket stuff
zscript: document, clean up, and sanity check websocket stuff
zscript: migrate user_bitmap to script object system
  Incidently, this increases the maximum number of user bitmaps to 256.

zscript: move zasm table and serialization to shared lib
zscript: remove separate zasm defines for compiler
zscript: improve location of many error messages
zscript: move internal symbols to new binding .zh files
  Internal symbols were defined in `.cpp` files, but now they are defined
  in `.zs` files with a new `internal` keyword and `@zasm` doc comments.
  These files live in `bindings`, and are automatically included for every
  compilation.
  
  This should simplify the process of adding new internal symbols. It also
  enables viewing documentation of any internal symbol in the VS Code
  extension (w/ a hover tooltip), and all the documentation we have
  for internal functions and variables has been inserted as comments in
  the binding files.
  
  This also deprecates functions that create an object of internal types,
  like `Game->CreateBitmap(width, height)` - and replaces with normal
  constructors - `new bitmap(width, height)`.

zscript: remove library symbol cpp files
zscript: remove internal class type
zscript: array literals better deduce their element types
zscript: type-safe opcode classes and use zasm table for stringification
zscript: separate zasm code from script data
zscript: make all command strings match enum name
  This normalizes all the string names for commands to be the same
  as the enum name used in code, and rewrites `get_script_command` to
  be an explicit mapping of this string representation and the enum value.
  These not matching was confusing, and was a potential source of future
  bugs.

zscript: remove unused features from ZScriptArray
  Various things are just a wrapper around what std::vector does,
  and we don't need dimensions, a template, or a custom iterator.

zscript: remove duplicated function deallocateZScriptArray
zscript: add -json switch to parser, use stderr for logging
zscript: add errors and warnings to json output as diagnostics
zscript: improve validation of file paths
  * `..` is now allowed in paths, as long as it doesn't reach above the
    quest's "Files" folder
  * more specific error messages when path is invalid
  * check for illegal filenames
  * general clean up of path validation code

zscript: remove `Game->Misc,Misc2,Misc3,STD,Ghost,Tango` arrays
  These have been removed from ZScript. Additionally their implementations
  are removed too, since no quests ever used them.

zscript: remove unused getters/setters from npcdata
zscript: simplify ltou, utol and convcase
  These functions returned false if the string given is empty. That's not
  useful, so now they longer return anything.

zscript: remove unnecessary `Debug->` "typecast" functions
zscript: remove many unused `Game->` functions
  All of the following functions were found to not be used in any
  published quests or scripts. Since they have all been deprecated (via
  `Screen->` or `mapadata`), they have been removed.
  
  * `GetScreenEnemy`
  * `SetScreenEnemy`
  * `GetScreenDoor`
  * `SetScreenDoor`
  * `SetScreenGuy`
  * `GetScreenGuy`
  * `SetScreenString`
  * `GetScreenString`
  * `SetScreenRoomType`
  * `GetScreenRoomType`
  * `SetScreenEntryX`
  * `GetScreenEntryX`
  * `SetScreenEntryY`
  * `GetScreenEntryY`
  * `SetScreenItem`
  * `GetScreenItem`
  * `SetScreenUndercombo`
  * `GetScreenUndercombo`
  * `SetScreenUnderCSet`
  * `GetScreenUnderCSet`
  * `SetScreenCatchall`
  * `GetScreenCatchall`
  * `SetScreenLayerOpacity`
  * `GetScreenLayerOpacity`
  * `SetScreenSecretCombo`
  * `GetScreenSecretCombo`
  * `SetScreenSecretCSet`
  * `GetScreenSecretCSet`
  * `SetScreenSecretFlag`
  * `GetScreenSecretFlag`
  * `SetScreenLayerMap`
  * `GetScreenLayerMap`
  * `SetScreenLayerScreen`
  * `GetScreenLayerScreen`
  * `SetScreenPath`
  * `GetScreenPath`
  * `SetScreenWarpReturnX`
  * `GetScreenWarpReturnX`
  * `SetScreenWarpReturnY`
  * `GetScreenWarpReturnY`
  * `GetScreenEFlags`

zscript: improve placement of errors/warnings on function calls
  Errors/warnings from using unknown or deprecated functions now attach to
  the function name, rather than the entire call expression.

zscript: remove A registers
  The "A" registers are two old registers that were never used in any
  quests. The only way to access them in recent builds has been the FFC
  `GetFFCInitA` / `SetFFCInitA`, but no published quest has ever used
  them.
  
  Now the ffc and item editors no longer display these two "A" register
  fields, and internally many other things handling these registers have
  been deleted.

zscript: make `ComboAt` internal as an optimization
zscript: tidy up std, move many enum/constants to bindings (#1065)
  Many constants are now named enums, and moved from `std` to binding
  files (which are imported automatically). This allows these values to be
  used with internal bindings (like `Game->`) without importing `std`; and
  improves documentation by giving everything a reference-able name.
  
  Also did a large general clean up of documentation comments, and made
  sure every internal function/variable documents what enum applies to it.
  
  Removed many unused or unimplemented features.

zscript: remove HEADER_GUARD (allow and ignore duplicate imports)
zscript: prevent parsing same files multiple times
  Before, every import would parse the file it included. Now, a cache is
  used so every file is only parsed once. This reduces compiler times (for
  example: compiling the binding files went from ~300ms to ~200ms).

zscript: remove WarpReturnC
zscript: deprecate `WrapAngle` (use `WrapRadians` instead)
zscript: emit shared zasm for scripts
  Previously each compiled script resulted in a separate chunk of zasm for
  just the code it used. This resulted in large amounts of duplicated zasm
  instructions across all scripts. Now, the zscript compiler uses the same
  chunk of zasm for all scripts.
  
  Compiling Yuurand's scripts went from 4m zasm commands to 1.87m.

zscript: remove separate "script UID" for sprites
  Every sprite object has an internal engine ID, but for some scripting
  features a separate "script UID" was used. Features such as `->HitBy`,
  `->ParentUID` used this - and `Screen->LoadNPCByUID` (etc.) existed to
  translate to the actual engine ID. These are relevant to `eweapon`,
  `lweapon` and `npc` sprites.
  
  Now, all these features just use the engine ID, and "script UID" is no
  longer a separate thing.

zscript: print context alongside script runtime errors
  When a script does something that results in an error, a message is
  logged to the console that usually mentions the zscript source variable
  or function where the problem occurred. This was usually right, but
  sometimes would mention the wrong thing.
  
  Some automation is added to more consistently print the correct variable
  or function name when encountering an error. In addition, potentially
  multiple bits of context may now print alongside error messages (one
  or all of: function, variable, or the name of a specific parameter).

zscript: add bounds check for `Screen->SideWarpID`, `mapdata::SideWarpID`
vscode: use new -json zscript option for diagnostics
vscode: improve symbol id generation used for metadata
vscode: add job pool, improve script processing

# Tests

save replay result file when starting replay
fix get_recent_release_tag method
update 'armos.zs' expected zasm
extract snapshot testing code to base class
add snapshot tests for x64 jit
fail replay test if jit cannot compile a script
fail on abort in web replay tests
stop on fatal errors in run_web_version.js
fix test_jit.py not looking in the right folder anymore
add "frames" and "length" meta fields to zplay

use "latest" version for all playground.qst replays
  These replays should be trivial to update as needed, and using no
  replay compat code makes them far more useful for verifying specific
  features.

add test_optimize_zasm.py and update_snapshots.py
move snapshot files to common folder
hash jit output for all but playground.qst
add scripts/run_for_every_qst.py, for running a command against every qst in the database
add replay test for class ctor/dtor
fix different outputs for jit snapshots
define calling convention for jit regression tests
add playground_exstate.zplay and playground_large_animations.zplay
add crucible_quest.zplay
add combo_rotator.zplay
  This is the first of more planned script replay tests, which along with
  the playground.qst replays will be re-compiled with the latest zscript
  in CI and their replays re-verified. Should result in better test
  coverage for newer compiler features.

fix crucible_quest.zplay from recent slope refactor
make the editor File->New test do what it says it does
use original qst for combo rotator replay
update crucible_quest.zplay for 5732b53
add local web app for running replay tests
move build archive operations to archive.py
  This greatly cleans up bisect_builds.py, which was doing a lot. Now
  a new tool `scripts/archive.py` is responsible for downloading any
  build - or building from source, as needed.

avoid enumerating s3 bucket just to download single version
use new archive download for run_replay_tests.py
  This removes the old download flow that required a github api key to
  get a baseline build for a compare report.

move github related functions to github_helpers.py
use requirements.txt for python deps
extract core replay runner to replays.py
format Python using Black
sort Python imports using isort
update expected zasm from recent changes
refactor how known replay failures are handled
import Path directly
remove solid.zplay
add playground_bitmap_credits.zplay
make some test scripts emit no warnings
update test scripts for new array syntax
add item_spawns_cloud.zplay
add crucible_quest_short_1.zplay
add snapshot test for compiling the zscript database
use truecolor bitmap for gfx frame hash
add "auto" script tests; reorganize and document playground quest
add scc replay test
add scripts/replay_uploads.py for downloading and running uploaded replays
add Database class to nicely automate downloading the database for local use
add pkmn.zplay for advanced SCC usage
vscode: add tests for 2.55 and 3.0
  Also:
  
  * On Mac, can now use path to `.app` bundle as installation folder
  * For 2.55, fix bug where parser errors underlined the entire line


# CI

reset cache for web builds
for web tests, build for RelWithDebInfo
include coverage for all apps, not just zplayer
bump build cache so 2.55 branch doesn't conflict
set correct test results folder for web replays for upload
get database update cron working again
fix type in download-artifact v4 action
fix another typo in download-artifact v4 action
set test results folder names for jit/non-jit replay runs
fix replay report generation for forks
upgrade to python 3.12.8
[win] update vcpkg to 2025.03.19

# Misc.

ignore fixup commits in git hook
use better examples for commit messages

version refactor
  Switch from "alpha/beta/gamma/final" to simpler "stable/nightly"
  versioning, and adopt a semver-like version string. See
  docs/versioning.md for more.
  
  Also, greatly automate the version bumping process. Simply select stable
  or nightly in GitHub action, and release scripts handle selecting the
  next version number accordingly.

link to website in generated changelog
Remove whistle delay for new replays
minor replay/debug improvements
add jit_runtime_debug.py for debugging jit bugs
remove noisy allocation logs
Revert "fix!: more lens/enemy stuff missed in prior commit"
  This reverts commit b9cafe07b073b7de07bb2f27207582bc8d435d62.

Revert "fix: changer ffcs counting for trigger groups and similar effects (they shouldn't)"
  This reverts commit 1a1bd37ac769db838a390e88b6fef03288576b01.

 fix(zq): upgrade combo 'move' code, more things now update on moving combos, add Combo overwrite warnings (#917)
Revert "ci!: use pull_request_target for ci.yml workflow"
  This reverts commit 40d18af060793860c7757fd69f8e5defd15e59c2.

Revert "refactor(zc): remove old movement functions"
  This reverts commit c5b8a0e17d165380f44ddf7950a73b6aa216d3ad.

zc: support "latest" for version field in zplay
zc: temporarily disable compare zasm opt as I improve in a branch
zc: disable zasm optimization until reported issues with tango are fixed
std_zh: update headers and bindings with array typing
zscript: optimize '!' operator
zscript: more parser optimizations
zscript: Optimize multiple consecutive PUSH opcodes
zscript: Add ZASM comments to parser output
zscript: add more ZASM comments
zscript: CALLFUNC/RETURNFUNC for optimized function call/return
zscript: New 'ReturnVisitor' pass errors on functions missing 'return' statements
zscript: code reachability optimizations
zscript: more small ZASM optimizations
zscript: add compile option for missing return error
zscript: treat empty functions like prototype functions
  This can be a heavy optimization for empty constructors/destructors.

zscript: make some more internal functions `constexpr`
zscript: re-number zasm comment numberings
zscript: Optimize 'STORED' followed by 'LOADD', and add 'STOREDV'
zscript: Optimize standing-statement x++,x-- to ++x,--x
zscript: optimize sections of zasm that are run only for their side-efffects
zscript: Optimizations related to function call parameters, unused function params
zscript: range loop cleanup
zscript: Optimize variables initialized to 0
zscript: clean up unused variables
zscript: internal label errors now prevent compile
zscript: compare related optimizations
zscript: allow 3-arg opcodes, move zasm tables to remove duplication
zscript: optimize comparisons to run when used, add CMP_BOOL flag for boolean-equality comparisons
zscript: support CMP_BOOL in x64 jit
zscript: add Region in anticipation of z3 scrolling
  To enable scripts to work in 2.55 and in a future release with z3
  scrolling, we are adding a non-functional `Region`:
  
  - `Region->Width`: width in pixels (today, always 256)
  - `Region->Height`: height in pixels (176)
  - `Region->ScreenWidth`: width in screens (1)
  - `Region->ScreenHeight`: height in screens (1)
  - `Region->NumCombos`: number of combo positions (176)
  - `Region->ID`: region id (0 - meaning just a normal 1x1 area)
  - `Region->OriginScreen`: top-left screen in the region
  - `Region->GetScreenForComboPos(int pos)`: given a combo position, returns the screen index associated with it
  - `Region->TriggerSecrets(int screen_index)`: trigger secrets for given screen. The screen must be in the region
  
  For example, if a script uses `Region->NumCombos` instead of `176` to
  iterate all the combos in the current area, it will work in both regions
  and non-regions. Additionally you should use `ComboAt(x, y)` rather than
  anything else to translate between pixel coordinates and a combo
  position.

zscript: optimize PEEK in x64 and wasm jit
zscript: use faster LOAD/STORE instead of LOADD/STORED in compiler
vscode: publish 1.0.8
vscode: publish 1.0.9
vscode: publish 1.0.10
vscode: publish 1.0.11